from bluetooth_attack.core import BTScanner
scanner = BTScanner()
# devices = scanner.scan_classic(duration=5)
# ble = scanner.scan_ble(duration=5)
print("Bluetooth scanner ready - requires hci0 interface")
